# Deploy configuration - copy to .deploy.env and fill in values
# This file is sourced by deploy scripts

# Main deploy (deploy.sh)
CLOUDFRONT_DISTRIBUTION_ID=your-cloudfront-distribution-id

# Lambda contact form (lambda/contact-form/deploy.sh)
FROM_EMAIL=your-from-email@example.com
TO_EMAIL=your-to-email@example.com
