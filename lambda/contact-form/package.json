{
  "name": "qcut-contact-form-lambda",
  "version": "1.0.0",
  "description": "Lambda function for handling qcut.app contact form submissions",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip index.js node_modules package.json"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
